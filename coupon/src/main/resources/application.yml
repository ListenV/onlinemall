server:
  port: 8070

spring:
  application:
    name: coupon
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.1.24:3306/mall_sms?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: sky03
    password: admin
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.1.24:8848
#      config:
  #        server-addr: 127.0.0.1:8848
#        import-check:
#          enabled: false
#  config:
#    import: nacos:coupon.properties
#    import: optional:nacos:instead
#spring.cloud.nacos.config.import-check.enabled=false

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      # 开启主键自增
      id-type: auto
